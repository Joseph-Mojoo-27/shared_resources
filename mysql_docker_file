FROM mariadb:10.6

# Install ca-certificates and mariadb-plugin-rocksdb, bypassing SSL verification
RUN apt-get update -o Acquire::https::Verify-Peer=false && \
    apt-get install -y ca-certificates && \
    apt-get install -y mariadb-plugin-rocksdb && \
    rm -rf /var/lib/apt/lists/*
